# server — implementation map

| Model | Code |
| --- | --- |
| serveStatic | `server.js:serveStatic` |
| isAllowed (path guard) | `server.js:isAllowed` |
| hostIsAllowed | `server.js:hostIsAllowed` |
| adapter allowlist | `server.js:listAdapterIds` |
| refresh / health routes | `server.js:/api/refresh`, `server.js:/api/health` |
